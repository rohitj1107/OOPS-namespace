namespace

namespace use for same class name and create object in drive class.
class name is same but file name is different and that file use 'namespace namespace_name',
starting on code after type or create class.

we can create object like $boj = new namespace_name\class_name;
